services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: plantlab
      POSTGRES_PASSWORD: plantlab
      POSTGRES_DB: plantlab
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: